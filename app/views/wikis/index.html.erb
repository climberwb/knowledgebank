<h1>All wikis</h1>
<% @wikis.each do |wiki| %> 
<div class = "media">
  <div class = "media-body">
    <div class = "media-heading">
       <p><%= link_to wiki.title, wiki_path(wiki.id)%></p>
     </div>
   </div>
 </div>
<%end%>

